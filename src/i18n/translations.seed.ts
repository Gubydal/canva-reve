import { defineMessages } from "react-intl";

// Seed messages so Canva translation upload has required content.
export const seedMessages = defineMessages({
  appTitle: {
    defaultMessage: "Creator Super Tool",
    description: "App name shown at the top of the design editor panel.",
  },
});
